%ul
  - @files.each do |file|
    %li
      = file.name
      %a(href="/files/#{file.name}")
        (SHOW)
      - if @user.signed_in?
        %a(href="/files/#{file.name}/edit")
          (EDIT)
        %form(method="post" action="/files/#{file.name}/delete" class="inline")
          %button(type="submit" name="delete")
            DELETE

- if @user.signed_in?
  %a(href="/files/new")
    + NEW
